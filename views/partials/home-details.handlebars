<div class="row cards">
    <div class="col s12 m6">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          <span class="card-title">{{user.user_name}}</span>
          <p class="title">{{title}}</p>
          <p>{{blog_content}}</p>
        </div>
        <div class="row">
          {{#each comments as |comment|}}
          <div class="col s12 m5">
      <div class="card-panel teal">
        <span class="white-text">
          <strong>{{user.user_name}}</strong>
          <p>{{comment_body}}</p>
        </span>
      </div>
    </div>
          {{/each}}
    <form class="comment-form col s12">
        <div class="input-field col s12">
          <textarea id="commentText" class="materialize-textarea commentArea"></textarea>
          <label for="textarea1">Say Something</label>
        </div>
      <button class="waves-effect waves-light btn-small">comment</button>
    </form>
  </div>
        
      </div>
    </div>
  </div>